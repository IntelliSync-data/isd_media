<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tag List View -->
    <record id="view_isd_media_tag_list" model="ir.ui.view">
        <field name="name">isd.media.tag.list</field>
        <field name="model">isd.media.tag</field>
        <field name="arch" type="xml">
            <list string="Tags" editable="bottom">
                <field name="name"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Tag Action -->
    <record id="action_isd_media_tag" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">isd.media.tag</field>
        <field name="view_mode">list</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first tag
            </p>
        </field>
    </record>
</odoo>
